# ============================================================
# Docker Build Context — only allow essentials
# ============================================================

# --- Ignore everything first ---
*

# --- Allow only what the image needs ---
!package.json
!package-lock.json
!src/
!docker/

# --- Always ignore ---
node_modules/
*.log
.git/
