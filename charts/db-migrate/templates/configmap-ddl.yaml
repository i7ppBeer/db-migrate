{{- if and .Values.ddl.enabled .Values.ddl.files }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "db-migrate.fullname" . }}-ddl
  labels:
    {{- include "db-migrate.labels" . | nindent 4 }}
    app.kubernetes.io/component: ddl
  {{- if eq .Values.mode "job" }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation
  {{- end }}
data:
  {{- range $filename, $content := .Values.ddl.files }}
  {{ $filename | quote }}: |
    {{- $content | nindent 4 }}
  {{- end }}
{{- end }}
