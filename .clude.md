# MongoDB Parity Plan (No MariaDB Changes)

## Goal
補齊 MongoDB 端目前落後於 MariaDB 的能力，並且全程不修改任何 MariaDB 檔案或流程。

## Scope Boundary
- 允許修改：
  - src/core/dcl-idempotent-checker.js
  - src/adapters/mongodb-adapter.js
  - test/dcl-idempotent-checker.test.js
  - test/mongodb-adapter.test.js
- 禁止修改：
  - src/adapters/mariadb-adapter.js
  - 所有 mariadb 相關 configs/migrations/tests

## Why This Plan
目前 MongoDB 與 MariaDB 主要差異在三塊：
1. DCL idempotency 比對深度不足（MongoDB 只比 privileges 數量，可能漏掉內容變化）。
2. MongoDB validate 缺少 JS 語法與 export 介面檢查（錯誤可能到執行時才發生）。
3. Sanity authoring 體驗未對齊（MariaDB 有明確 sanity 區塊，MongoDB 端應補模板引導）。

## Phase 1 - Stronger MongoDB DCL Idempotency
### Implementation
1. 更新 src/core/dcl-idempotent-checker.js 的 captureMongoDBState():
- roles[].privileges 由 count 改為 canonicalized privilege objects。
- 新增比較欄位：authenticationRestrictions、customData（僅納入穩定欄位）。
- 針對 users/roles/privileges 進行穩定排序，避免非語義順序造成誤判。

2. 比對策略維持 compareStates()，但輸入 state 內容更精確。

### Acceptance Criteria
- 權限數量相同但內容不同，必須判定為差異。
- customData 有差異，必須判定為差異。
- 純時間戳差異，不應判定為差異。

### Tests
在 test/dcl-idempotent-checker.test.js 新增：
- Mongo role privileges same count, different actions -> fail。
- Mongo user customData changed -> fail。
- Stable sorted output snapshot-like assertion。

## Phase 2 - MongoDB Validate Syntax and Interface Guardrails
### Implementation
1. 在 src/adapters/mongodb-adapter.js 新增 validateJSSyntax(content, fileName, mode):
- 解析 JS syntax（使用 parser，或保守 fallback 的 Function parse）。
- 檢查 migration export 介面：
  - 必須有 up。
  - 若 mode=versioned，必須有 down。
  - 若 mode=repeatable，down 可省略。

2. 在 validateContent() 中整合 syntax/interface 檢查結果：
- 回傳 errors/warnings 併入既有 forbidden/dangerous/warning 結果。
- 不改現有規則碼語義，僅新增 syntax/interface 類錯誤碼。

### Acceptance Criteria
- JS syntax 錯誤在 validate 階段就被攔截。
- 缺 up 一律 fail。
- DDL 缺 down fail；DCL 缺 down pass。

### Tests
在 test/mongodb-adapter.test.js 新增：
- syntax error file -> validateContent invalid。
- no up export -> invalid。
- versioned no down -> invalid。
- repeatable no down -> valid（若無其他違規）。

## Phase 3 - Sanity Authoring Parity (Low Risk)
### Implementation
1. 只修改 src/adapters/mongodb-adapter.js 的 create()/createDCL() 產生模板：
- 預設附上 preCheck/postCheck 範例函式。
- 在模板註解明確寫明：
  - preCheck 用於執行前前置條件。
  - postCheck 用於執行後一致性檢查。
  - 失敗時搭配 sanity checker 可觸發 rollback。

2. 不導入註解式 DSL parser（避免大改、降低風險）。

### Acceptance Criteria
- 新建立 Mongo migration 預設就有 preCheck/postCheck scaffolding。
- 不影響既有 migration 檔案格式。

### Tests
在 test/mongodb-adapter.test.js 新增：
- create() 生成檔應包含 preCheck/postCheck。
- createDCL() 生成檔應包含 preCheck/postCheck。

## Verification Plan (Must Run)
1. 單元測試：
- npx vitest run test/dcl-idempotent-checker.test.js test/mongodb-adapter.test.js test/sanity-checker.test.js

2. 實機流程測試（若 MongoDB 可連線）：
- node src/cli.js test-all --pattern "databases/mongodb/**/config.js" --console-only --sanity-check

3. 邊界驗證（不可修改 MariaDB）：
- git diff --name-only
- 確認輸出不包含任何 mariadb 檔案。

## Execution Order
1. Phase 1 + tests
2. Phase 2 + tests
3. Phase 3 + tests
4. Full verification run
5. Commit and push

## Rollback Strategy
- 每個 phase 完成即跑對應測試。
- 若該 phase 測試失敗，先修該 phase，不進下一階段。
- 若 full verification fail，優先檢查新加語法檢查與 state canonicalization。

## Deliverables
- MongoDB parity 功能完成（僅 MongoDB 路徑）
- 測試覆蓋補齊
- 完整驗證輸出與 commit/push 記錄
